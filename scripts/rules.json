{
    "predicate": "all",
    "rules": [
        {
            "field": "Date Received",
            "predicate": "greater_than",
            "value": 2
        }
    ],
    "actions": [
        {
            "action_type": "MARK_AS_READ"
        },
        {
            "action_type": "MOVE_MESSAGE",
            "folder_name": "Important"
        }
    ]
}